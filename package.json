{
  "name": "claude-code-hwp-plugin",
  "version": "0.8.2",
  "description": "Claude Code plugin for HWP document automation. 118 Python RPC handlers + MCP tools + workflow commands + HWPX rule hooks. v0.8.2 rule-based form filling engine (Phase 5A-G, form_fill_auto composite pipeline).",
  "type": "module",
  "private": true,
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@xmldom/xmldom": "^0.8.11",
    "jszip": "^3.10.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
