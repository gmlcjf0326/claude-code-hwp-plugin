# HWP Studio MCP Server — Python dependencies (v0.7.4.9 pinned)
#
# ⚠️ 권장 Python: 3.13 LTS (python.org 공식)
#    Python 3.14 에서 pywin32 gen_py 최초 import 가 수 시간 hang 하는 이슈 있음.
#    설치: py -3.13 -m pip install -r requirements.txt
#    또는: "C:\Path\To\Python3.13\python.exe" -m pip install -r requirements.txt
#
# 전체 버전 고정 이유: 다른 PC/사용자 환경에서 재현 가능한 안정 조합 보장.
# v0.7.4.9: Python 3.13 에서 실제 smoke test + 설치 검증 완료된 조합

# === Core HWP automation ===
pyhwpx==1.7.2
pywin32==308
# pywin32 > 308 은 Python 3.14 gen_py hang 유발 가능 — 308 고정

# === Reference material parsing ===
PyMuPDF==1.24.11
openpyxl==3.1.5
python-docx==1.1.2

# === v0.7.4.2: PDF Clone 기본 의존성 (native PDF 지원) ===
# v0.7.4.9: Python 3.13 에서 실제 설치된 버전으로 업데이트 (numpy 2.x 지원)
pdfplumber>=0.11.4
Pillow>=10.4.0
opencv-python-headless>=4.10.0.84,<5.0.0
numpy>=1.26.4
# 주: numpy 2.x 는 paddlepaddle 3.x 와 호환 (2.6.1 제약 해제)

# === v0.7.4.3 → v0.7.4.9: OCR 엔진 (스캔 PDF 시 lazy-import) ===
# v0.7.4.9 변경: paddlepaddle 2.6.1 은 Python 3.13 미지원 → 3.0.0+ 필수
# 최초 실행 시 ~150MB 모델이 ~/.paddleocr 에 자동 다운로드됩니다 (corporate proxy 주의)
# API 차이: 3.x 는 use_angle_cls 대신 use_textline_orientation, show_log 제거
paddlepaddle>=3.0.0
paddleocr>=3.0.0
